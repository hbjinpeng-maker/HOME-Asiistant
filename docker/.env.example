# 复制为 .env 后修改
# cp .env.example .env

# 时区（中国用户建议 Asia/Shanghai）
TZ=Asia/Shanghai

# Home Assistant 配置目录（宿主机路径）
HA_CONFIG_DIR=/data/homeassistant

# Home Assistant 镜像版本（建议固定版本，不要用 latest）
HA_IMAGE=ghcr.io/home-assistant/home-assistant:stable
